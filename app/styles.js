import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    containerMain: {
        height: '100vh',
        width: '100vw',
        flex: 1,
        padding: 10,
        overflowY: 'scroll',
    },
})

export default styles